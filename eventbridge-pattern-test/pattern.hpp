#include <strings>

typedef  std::string PatternValue;

class Pattern {
    private:
    /**
    * Represents the pattern value th
     */
       PatternValue patternString;

    Pattern(std::string)
}